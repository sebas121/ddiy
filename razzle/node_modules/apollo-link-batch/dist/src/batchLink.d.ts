import { ApolloLink, Operation, FetchResult, Observable, NextLink } from 'apollo-link-core';
import { BatchHandler } from './batching';
export { OperationBatcher, BatchableRequest, BatchHandler } from './batching';
export default class BatchLink extends ApolloLink {
    private batchInterval;
    private batchMax;
    private batcher;
    constructor(fetchParams: {
        batchInterval?: number;
        batchMax?: number;
        batchHandler: BatchHandler;
    });
    request(operation: Operation, forward?: NextLink): Observable<FetchResult> | null;
}
